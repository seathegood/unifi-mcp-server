# Agent Guide

Repository workflow and standards for coding agents are defined in:

- `docs/AGENTS.md`

Use `make` targets as the primary local contract (`bootstrap`, `doctor`, `check`, `lint`, `format`, `format-check`, `unit`, `clean-local`).
